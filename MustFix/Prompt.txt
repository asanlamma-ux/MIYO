‎some issues with reading space, like for example tapping a word that was overwritten with a term, the app doesn't care and just goes on and activates the tap to scroll feature, even though I am tapping a term to see what it's original is. there's also the issue of tap to scroll, add a scroll animation, the current one is instant, making it confusing. fix and improve the group term export, it doesn't work, add it so I can choose what group term to export. there's also that button next to opds catalog, the File Button one. even though I have already set up the Folder the app will use, to store data. it's still there and doesn't disappear. on book details, table of contents. add a dropdown button so I can see all the chapters and click on it. also add a json based save progress, save that data inside the root directory of the app. for storing, bookmark data, last chapter and percentage, it's essentially the memory of the app, your progress, make it secure. add categories, maker and Selection. inside Library tab. and while at it, remake the inside UI of library tab. it's messy right now. also add it so I can hold to select and delete history on history tab, without affecting progress or last read chapters inside books. delete the blue light filter on settings, app isn't quite running, like I want it so that when I'm on my download directory. I can just tap this epub file, tap open and it will redirect me into the app. but the app doesn't seem to detect this. the app also doesn't request or send storage access permission request. the email confirmation button also doesn't work, and I am content with the email confirmation template. the problem seems to be with the logic or script, there's also the welcome to miyo modal, there's two buttons. look at the attached screenshots, create term modal inside reading space is also unstable. also update and improve the terms logic and function. for example on MTL. there's this name called xinnai, I added a term for it called Apple but there's a slight problem. there are cases where it's connected to another word like for example, jiu xinnai. since term is activated, it is like jiu apple, but we don't want that. add it so that I can hold to select the two. but it doesn't select the added term okay? in the system, the selected word automatically becomes what they're original is. for example in jiu apple, since in the apple term, it's original raw without term is xinnai, the system should select the two as jiu xinnai. also fix the sleep timer, it doesn't work. as well as the page animation, curl, fade doesn't work, and book mark. and finally, add the google drive Oauth back, Google Drive sync too. google login.
